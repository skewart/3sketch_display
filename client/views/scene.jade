// Mixins
mixin sliderContainer
  #sliderContainer(style="background-color:red; width:200px; height:100px;") This is sliderContainer --> #{js_code} 
    
mixin sliderBox
  #sliderBox(style="background-color:blue; width:300px; height:100px;") This is sliderBox --> #{ params }


// Document
doctype html
html(lang='en')
  head
    title Nothing to see here.
  body(style="margin:0px;")
    #canvasGoesHere(style="width:100%; height: 100%; position:absolute;")
    script( src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r67/three.min.js" )
    script( src="/js/three_orbitcontrols.js" )
    script( src="https://cdnjs.cloudflare.com/ajax/libs/dat-gui/0.5/dat.gui.min.js" )
    !{ js_code }
    // This is just temporary, for initial development
    script.
           var _paramslist = !{ geoParams },
                _functext = "!{ jsCode }";
    script( src="/js/3sketch_display.js" )